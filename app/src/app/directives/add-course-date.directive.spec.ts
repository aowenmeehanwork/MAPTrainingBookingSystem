import { AddCourseDateDirective } from './add-course-date.directive';

describe('AddCourseDateDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCourseDateDirective();
    expect(directive).toBeTruthy();
  });
});
