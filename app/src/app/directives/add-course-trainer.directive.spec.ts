import { AddCourseTrainerDirective } from './add-course-trainer.directive';

describe('AddCourseTrainerDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCourseTrainerDirective();
    expect(directive).toBeTruthy();
  });
});
