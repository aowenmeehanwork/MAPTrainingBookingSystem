import { AddCourseAimDirective } from './add-course-aim.directive';

describe('AddCourseAimDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCourseAimDirective();
    expect(directive).toBeTruthy();
  });
});
