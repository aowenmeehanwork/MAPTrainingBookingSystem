import { AddCourseDescriptionDirective } from './add-course-description.directive';

describe('AddCourseDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCourseDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
