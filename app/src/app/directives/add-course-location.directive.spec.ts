import { AddCourseLocationDirective } from './add-course-location.directive';

describe('AddCourseLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCourseLocationDirective();
    expect(directive).toBeTruthy();
  });
});
